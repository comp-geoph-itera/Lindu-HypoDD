A directory for the binary files after compiling in this software. The compilers are located in `mingw64` folder. Due to Windows OS users only, this folder only becomes a place for the `*.exe` files such as:

  - HypoDD.exe
  - ph2dt.exe
